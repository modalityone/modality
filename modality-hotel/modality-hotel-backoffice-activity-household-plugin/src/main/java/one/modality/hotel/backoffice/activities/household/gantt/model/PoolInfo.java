package one.modality.hotel.backoffice.activities.household.gantt.model;

/**
 * Lightweight pool info for filter buttons.
 * Avoids direct dependency on entity classes in UI code.
 *
 * @author Claude Code Assistant
 */
public record PoolInfo(Object id, String name, String webColor) {
}
