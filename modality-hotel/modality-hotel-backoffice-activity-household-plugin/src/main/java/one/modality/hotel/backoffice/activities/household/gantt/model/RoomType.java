package one.modality.hotel.backoffice.activities.household.gantt.model;

/**
 * Room type enumeration.
 * Differentiates between single-bed rooms (bookings shown on room row)
 * and multi-bed rooms (bookings shown on individual bed rows).
 */
public enum RoomType {
    SINGLE_BED,
    MULTI_BED
}
