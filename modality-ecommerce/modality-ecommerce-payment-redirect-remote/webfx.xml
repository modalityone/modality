<?xml version="1.0" encoding="UTF-8"?>
<project>

    <description>The client-side implementation of the redirect payment API (just forwards the request to the server).</description>

    <exported-packages>
        <source-packages/>
    </exported-packages>

    <dependencies>
        <used-by-source-modules/>
    </dependencies>

    <providers>
        <provider interface="one.modality.ecommerce.payment.redirect.spi.RedirectPaymentProvider">one.modality.ecommerce.payment.redirect.spi.impl.remote.RemoteRedirectPaymentProvider</provider>
    </providers>

</project>