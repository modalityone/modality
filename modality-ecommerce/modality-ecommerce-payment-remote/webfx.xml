<?xml version="1.0" encoding="UTF-8"?>
<project>

    <description>The client-side implementation of the embedded payment API (just forwards the request to the server).</description>

    <exported-packages>
        <source-packages/>
    </exported-packages>

    <dependencies>
        <used-by-source-modules/>
    </dependencies>

    <providers>
        <provider interface="one.modality.ecommerce.payment.spi.PaymentServiceProvider">one.modality.ecommerce.payment.spi.impl.remote.RemotePaymentServiceProvider</provider>
    </providers>

</project>