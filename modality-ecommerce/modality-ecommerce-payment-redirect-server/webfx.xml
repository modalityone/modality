<?xml version="1.0" encoding="UTF-8"?>
<project>

    <description>The actual server-side implementation of the redirect payment API, which decides which payment gateway to use and calls it.</description>

    <exported-packages>
        <source-packages/>
    </exported-packages>

    <dependencies>
        <used-by-source-modules/>
    </dependencies>

    <providers>
        <provider interface="one.modality.ecommerce.payment.redirect.spi.RedirectPaymentProvider">one.modality.ecommerce.payment.redirect.spi.impl.server.ServerRedirectPaymentProvider</provider>
    </providers>

</project>