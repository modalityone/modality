{
  "serverHost": "${{ BUS_SERVER_HOST | 'localhost' }}",
  "serverPort": "${{ BUS_SERVER_PORT | 80 }}",
  "serverSSL": "${{ BUS_SERVER_SSL | false }}",
  "busPrefix": "${{ BUS_PREFIX | 'eventbus' }}",
  "pingInterval": "${{ BUS_PING_INTERVAL | 5000 }}"
}