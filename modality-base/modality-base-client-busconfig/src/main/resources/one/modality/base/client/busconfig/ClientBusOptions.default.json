{
  "serverHost": "${{ SERVER_HOST | 'localhost' }}",
  "serverPort": "${{ SERVER_PORT | 8080 }}",
  "serverSSL": "${{ SERVER_SSL | false }}",
  "busPrefix": "${{ BUS_PREFIX | 'eventbus' }}",
  "pingInterval": "${{ BUS_PING_INTERVAL | 5000 }}"
}