<?xml version="1.0" encoding="UTF-8"?>
<project>

    <description>The login activity displayed by the UI router to an unauthenticated user trying to access a route requiring authorization.</description>

    <exported-packages>
        <source-packages/>
    </exported-packages>

    <dependencies>
        <used-by-source-modules/>
    </dependencies>

    <providers>
        <provider interface="dev.webfx.stack.routing.uirouter.UiRoute">one.modality.crm.client.activities.login.LoginUiRoute</provider>
    </providers>

</project>